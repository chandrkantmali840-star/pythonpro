import { topicArticle } from "./helpers";

type Builtin = {
  name: string;
  purpose: string;
  signature: string;
  parameters: string;
  returns: string;
  example: string;
  output: string;
  mistake: string;
  level?: "Beginner" | "Intermediate";
};
const builtins: Builtin[] = [
  {
    name: "print",
    purpose: "Writes text representations of objects to a stream.",
    signature: "print(*objects, sep=' ', end='\\n', file=None, flush=False)",
    parameters:
      "Any objects plus optional separator, ending, target stream, and flushing flag.",
    returns: "None.",
    example: "print('A', 'B', sep='-', end='!')",
    output: "A-B!",
    mistake: "Expecting print() to return the displayed text.",
  },
  {
    name: "input",
    purpose: "Reads one line of user input as a string.",
    signature: "input(prompt='')",
    parameters: "An optional prompt string.",
    returns: "The entered text without the trailing newline.",
    example: "name = 'Asha'  # imagine input() returned this\nprint(name)",
    output: "Asha",
    mistake: "Forgetting to convert text before numeric arithmetic.",
  },
  {
    name: "len",
    purpose: "Returns the number of items in an object.",
    signature: "len(object)",
    parameters: "An object implementing __len__.",
    returns: "A non-negative integer.",
    example: "print(len({'a': 1, 'b': 2}))",
    output: "2",
    mistake: "Using len(value) on an integer.",
  },
  {
    name: "type",
    purpose: "Returns an object's type.",
    signature: "type(object)",
    parameters: "One object in the inspection form.",
    returns: "The object's class.",
    example: "print(type(3.5).__name__)",
    output: "float",
    mistake:
      "Using type(x) == Base when isinstance() should accept subclasses.",
  },
  {
    name: "int",
    purpose: "Constructs an integer from a number or compatible string.",
    signature: "int(value=0, base=10)",
    parameters: "A number or string; base applies to string conversion.",
    returns: "An int.",
    example: "print(int('101', 2))",
    output: "5",
    mistake: "Passing a decimal string such as '3.5' directly.",
  },
  {
    name: "float",
    purpose: "Constructs a floating-point number.",
    signature: "float(value=0.0)",
    parameters: "A number or numeric string.",
    returns: "A float.",
    example: "print(float('2.5') + 1)",
    output: "3.5",
    mistake: "Expecting exact decimal representation for every fraction.",
  },
  {
    name: "str",
    purpose: "Returns a readable string representation of an object.",
    signature: "str(object='')",
    parameters: "Any object.",
    returns: "A str.",
    example: "print('Score: ' + str(9))",
    output: "Score: 9",
    mistake: "Confusing str bytes with decoded text.",
  },
  {
    name: "list",
    purpose: "Constructs a mutable list from an iterable.",
    signature: "list(iterable=())",
    parameters: "An optional iterable.",
    returns: "A new list.",
    example: "print(list(range(3)))",
    output: "[0, 1, 2]",
    mistake: "Assuming list(existing) performs a deep copy.",
  },
  {
    name: "tuple",
    purpose: "Constructs an immutable tuple from an iterable.",
    signature: "tuple(iterable=())",
    parameters: "An optional iterable.",
    returns: "A tuple.",
    example: "print(tuple('ab'))",
    output: "('a', 'b')",
    mistake: "Writing (1) instead of (1,) for one item.",
  },
  {
    name: "set",
    purpose: "Constructs a mutable set of unique hashable elements.",
    signature: "set(iterable=())",
    parameters: "An optional iterable.",
    returns: "A set.",
    example: "print(len(set([1, 1, 2])))",
    output: "2",
    mistake: "Using {} for an empty set.",
  },
  {
    name: "dict",
    purpose:
      "Constructs a dictionary from mappings, key-value pairs, or keyword arguments.",
    signature: "dict(mapping_or_iterable=(), **kwargs)",
    parameters: "A mapping, iterable of pairs, and/or keyword pairs.",
    returns: "A dict.",
    example: "print(dict([('a', 1), ('b', 2)]))",
    output: "{'a': 1, 'b': 2}",
    mistake: "Supplying iterable items that do not contain exactly two values.",
  },
  {
    name: "range",
    purpose: "Creates an immutable lazy arithmetic progression of integers.",
    signature: "range(stop) or range(start, stop, step)",
    parameters: "Integer start, exclusive stop, and non-zero step.",
    returns: "A range object.",
    example: "print(list(range(2, 7, 2)))",
    output: "[2, 4, 6]",
    mistake: "Expecting the stop value to be included.",
  },
  {
    name: "sum",
    purpose: "Adds numeric items from an iterable plus an optional start.",
    signature: "sum(iterable, start=0)",
    parameters: "An iterable and compatible starting value.",
    returns: "The accumulated total.",
    example: "print(sum([2, 3, 4], 1))",
    output: "10",
    mistake: "Using sum() to concatenate strings.",
  },
  {
    name: "min",
    purpose: "Returns the smallest item or item with the smallest key.",
    signature: "min(iterable, *, key=None, default=...) ",
    parameters: "An iterable or multiple arguments; optional key and default.",
    returns: "The minimum selected item.",
    example: "print(min(['pear', 'fig'], key=len))",
    output: "fig",
    mistake: "Calling min() on an empty iterable without default.",
  },
  {
    name: "max",
    purpose: "Returns the largest item or item with the largest key.",
    signature: "max(iterable, *, key=None, default=...)",
    parameters: "An iterable or multiple arguments; optional key and default.",
    returns: "The maximum selected item.",
    example: "print(max(['pear', 'fig'], key=len))",
    output: "pear",
    mistake: "Expecting key= to transform the returned item.",
  },
  {
    name: "abs",
    purpose: "Returns the absolute value or magnitude.",
    signature: "abs(number)",
    parameters: "A real or complex number implementing __abs__.",
    returns: "A non-negative real value.",
    example: "print(abs(-7), abs(3+4j))",
    output: "7 5.0",
    mistake: "Assuming abs() mutates its argument.",
  },
  {
    name: "round",
    purpose: "Rounds a number to a requested decimal precision.",
    signature: "round(number, ndigits=None)",
    parameters: "A number and optional digit count.",
    returns: "A rounded number; exact tie behavior follows the numeric type.",
    example: "print(round(2.675, 2))",
    output: "2.67",
    mistake:
      "Expecting binary floating-point decimal ties to behave like exact decimal arithmetic.",
  },
  {
    name: "sorted",
    purpose: "Returns a new sorted list from an iterable.",
    signature: "sorted(iterable, *, key=None, reverse=False)",
    parameters: "An iterable plus optional key function and direction.",
    returns: "A new list.",
    example: "print(sorted(['pear', 'fig'], key=len))",
    output: "['fig', 'pear']",
    mistake: "Expecting the input list to be changed.",
  },
  {
    name: "reversed",
    purpose: "Returns a reverse iterator over a reversible object.",
    signature: "reversed(sequence)",
    parameters: "An object supporting __reversed__ or sequence protocol.",
    returns: "An iterator.",
    example: "print(list(reversed([1, 2, 3])))",
    output: "[3, 2, 1]",
    mistake: "Expecting a list instead of an iterator.",
  },
  {
    name: "enumerate",
    purpose: "Pairs iterable items with a counter.",
    signature: "enumerate(iterable, start=0)",
    parameters: "An iterable and starting integer.",
    returns: "An enumerate iterator of (index, item) tuples.",
    example: "print(list(enumerate(['a', 'b'], start=1)))",
    output: "[(1, 'a'), (2, 'b')]",
    mistake: "Maintaining a separate counter when enumerate() is clearer.",
  },
  {
    name: "zip",
    purpose: "Combines items from iterables position by position.",
    signature: "zip(*iterables, strict=False)",
    parameters: "Zero or more iterables and optional strict length checking.",
    returns: "An iterator of tuples.",
    example: "print(list(zip(['a', 'b'], [1, 2])))",
    output: "[('a', 1), ('b', 2)]",
    mistake:
      "Ignoring silent truncation; use strict=True when lengths must match.",
  },
  {
    name: "map",
    purpose: "Lazily applies a function to items from one or more iterables.",
    signature: "map(function, iterable, *iterables)",
    parameters: "A callable and iterable inputs.",
    returns: "A map iterator.",
    example: "print(list(map(str.upper, ['a', 'b'])))",
    output: "['A', 'B']",
    mistake: "Expecting eager execution without consuming the iterator.",
  },
  {
    name: "filter",
    purpose: "Lazily keeps iterable items accepted by a predicate.",
    signature: "filter(function, iterable)",
    parameters: "A predicate or None and an iterable.",
    returns: "A filter iterator.",
    example: "print(list(filter(str.isdigit, ['2', 'x', '4'])))",
    output: "['2', '4']",
    mistake: "Forgetting the result is lazy.",
  },
  {
    name: "any",
    purpose: "Returns True when at least one iterable item is truthy.",
    signature: "any(iterable)",
    parameters: "An iterable.",
    returns: "A bool; False for an empty iterable.",
    example: "print(any([0, '', 5]))",
    output: "True",
    mistake: "Confusing any() with all().",
  },
  {
    name: "all",
    purpose: "Returns True when every iterable item is truthy.",
    signature: "all(iterable)",
    parameters: "An iterable.",
    returns: "A bool; True for an empty iterable.",
    example: "print(all([1, True, 'x']))",
    output: "True",
    mistake: "Forgetting that all([]) is True.",
  },
  {
    name: "open",
    purpose: "Opens a file and returns a file object.",
    signature: "open(file, mode='r', encoding=None, ...)",
    parameters: "A path plus mode, encoding, and other I/O options.",
    returns: "A text or binary file object.",
    example:
      "from io import StringIO\nfile = StringIO('hello')\nprint(file.read())",
    output: "hello",
    mistake:
      "Omitting an explicit text encoding for portable application data.",
  },
  {
    name: "isinstance",
    purpose:
      "Checks whether an object is an instance of a class or accepted union/tuple of classes.",
    signature: "isinstance(object, classinfo)",
    parameters: "An object and type specification.",
    returns: "A bool.",
    example: "print(isinstance(True, int))",
    output: "True",
    mistake: "Forgetting bool is a subclass of int.",
  },
  {
    name: "help",
    purpose: "Displays interactive documentation for an object or topic.",
    signature: "help(request=None)",
    parameters: "An object or documentation-topic string.",
    returns: "Normally None after printing help.",
    example: "print(str.split.__name__)",
    output: "split",
    mistake: "Using help() as though it returns the documentation string.",
  },
];

export const builtinArticles = builtins.map((item) =>
  topicArticle({
    title: `${item.name}()`,
    definition: item.purpose,
    category: "Built-in Functions",
    level: item.level,
    kind: "Function",
    syntax: item.signature,
    example: item.example,
    output: item.output,
    details: [
      `Parameters: ${item.parameters}`,
      `Return value: ${item.returns}`,
      "This built-in is available without importing a module.",
    ],
    why: `Use ${item.name}() when its documented operation matches the program's intent.`,
    realWorld: `Apply ${item.name}() in data validation, transformation, display, or control flow as appropriate.`,
    related: ["Built-in Functions", "Python Fundamentals"],
    mistakes: [
      item.mistake,
      "Passing an object that does not support the required protocol.",
    ],
    operations: [
      { label: "Parameters", meaning: item.parameters },
      { label: "Returns", meaning: item.returns },
      {
        label: "Call",
        meaning: item.purpose,
        example: item.signature,
        result: item.output,
      },
    ],
    keywords: [item.name, item.signature, item.purpose],
  }),
);

export const builtinsOverview = topicArticle({
  title: "Built-in Functions",
  definition:
    "Built-in functions and constructors are available in every normal Python module without an import.",
  category: "Built-in Functions",
  kind: "Function",
  syntax: "result = function(arguments)",
  example:
    "values = [3, 1, 2]\nprint(len(values), min(values), sorted(values))",
  output: "3 1 [1, 2, 3]",
  details: [
    "Built-ins work through common object protocols such as iteration, length, comparison, conversion, and representation.",
    "Constructors such as list(), dict(), int(), and str() create or convert values.",
    "Iterator-returning built-ins such as zip(), map(), filter(), enumerate(), and reversed() are lazy.",
  ],
  why: "They provide concise, consistent operations shared by Python objects.",
  realWorld:
    "Validate types, summarize collections, transform iterables, and perform file I/O.",
  related: ["Data Types", "Functions", "Iterators"],
  keywords: builtins.flatMap((x) => [x.name, x.signature]),
});
