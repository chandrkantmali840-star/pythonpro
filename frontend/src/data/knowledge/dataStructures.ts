import { methodArticle, topicArticle } from "./helpers";
import type { MethodReference, ReferenceRow } from "./types";

const listMethods: MethodReference[] = [
  {
    name: "append()",
    purpose: "Adds one item to the end of a list.",
    syntax: "list.append(item)",
    example: "numbers = [10, 20]\nnumbers.append(30)\nprint(numbers)",
    result: "[10, 20, 30]",
    note: "Mutates the list and returns None.",
  },
  {
    name: "extend()",
    purpose: "Adds every item from an iterable to the end.",
    syntax: "list.extend(iterable)",
    example: "a = [1, 2]\na.extend([3, 4])\nprint(a)",
    result: "[1, 2, 3, 4]",
    note: "Unlike append(), it adds the iterable's items individually.",
  },
  {
    name: "insert()",
    purpose: "Inserts one item before a given index.",
    syntax: "list.insert(index, item)",
    example: "a = ['a', 'c']\na.insert(1, 'b')\nprint(a)",
    result: "['a', 'b', 'c']",
  },
  {
    name: "remove()",
    purpose: "Removes the first item equal to a value.",
    syntax: "list.remove(value)",
    example: "a = [2, 1, 2]\na.remove(2)\nprint(a)",
    result: "[1, 2]",
    note: "Raises ValueError when the value is absent.",
  },
  {
    name: "pop()",
    purpose: "Removes and returns an item; the last item is the default.",
    syntax: "list.pop(index=-1)",
    example: "a = [10, 20, 30]\nitem = a.pop()\nprint(item, a)",
    result: "30 [10, 20]",
    note: "Raises IndexError on an empty list or invalid index.",
  },
  {
    name: "clear()",
    purpose: "Removes every item in place.",
    syntax: "list.clear()",
    example: "a = [1, 2]\na.clear()\nprint(a)",
    result: "[]",
  },
  {
    name: "index()",
    purpose: "Returns the first index of a matching value.",
    syntax: "list.index(value, start=0, stop=len(list))",
    example: "a = ['x', 'y', 'x']\nprint(a.index('x', 1))",
    result: "2",
    note: "Raises ValueError when no match exists.",
  },
  {
    name: "count()",
    purpose: "Counts items equal to a value.",
    syntax: "list.count(value)",
    example: "print([1, 2, 1].count(1))",
    result: "2",
  },
  {
    name: "sort()",
    purpose: "Sorts the list in place.",
    syntax: "list.sort(*, key=None, reverse=False)",
    example: "a = ['pear', 'fig', 'apple']\na.sort(key=len)\nprint(a)",
    result: "['fig', 'pear', 'apple']",
    note: "Returns None; use sorted() when a new list is needed.",
  },
  {
    name: "reverse()",
    purpose: "Reverses list order in place.",
    syntax: "list.reverse()",
    example: "a = [1, 2, 3]\na.reverse()\nprint(a)",
    result: "[3, 2, 1]",
  },
  {
    name: "copy()",
    purpose: "Returns a shallow copy of the list.",
    syntax: "list.copy()",
    example: "a = [[1], [2]]\nb = a.copy()\nb.append([3])\nprint(a, b)",
    result: "[[1], [2]] [[1], [2], [3]]",
    note: "Nested mutable objects are still shared.",
  },
];

const stringMethods: MethodReference[] = [
  [
    "upper()",
    "Returns an uppercase copy.",
    "text.upper()",
    "print('Py'.upper())",
    "PY",
  ],
  [
    "lower()",
    "Returns a lowercase copy.",
    "text.lower()",
    "print('Py'.lower())",
    "py",
  ],
  [
    "title()",
    "Title-cases words in a copy.",
    "text.title()",
    "print('python guide'.title())",
    "Python Guide",
  ],
  [
    "capitalize()",
    "Capitalizes the first character and lowercases the rest.",
    "text.capitalize()",
    "print('pYTHON'.capitalize())",
    "Python",
  ],
  [
    "strip()",
    "Removes leading and trailing characters; whitespace by default.",
    "text.strip(chars=None)",
    "print('  hi  '.strip())",
    "hi",
  ],
  [
    "lstrip()",
    "Removes characters from the left edge.",
    "text.lstrip(chars=None)",
    "print('  hi'.lstrip())",
    "hi",
  ],
  [
    "rstrip()",
    "Removes characters from the right edge.",
    "text.rstrip(chars=None)",
    "print('hi  '.rstrip())",
    "hi",
  ],
  [
    "replace()",
    "Returns a copy with occurrences replaced.",
    "text.replace(old, new, count=-1)",
    "print('one one'.replace('one', 'two', 1))",
    "two one",
  ],
  [
    "split()",
    "Splits text into a list using a separator.",
    "text.split(sep=None, maxsplit=-1)",
    "print('a,b,c'.split(','))",
    "['a', 'b', 'c']",
  ],
  [
    "join()",
    "Joins string items with the receiver as separator.",
    "separator.join(iterable)",
    "print('-'.join(['a', 'b']))",
    "a-b",
  ],
  [
    "find()",
    "Returns the lowest match index or -1.",
    "text.find(sub)",
    "print('banana'.find('na'))",
    "2",
  ],
  [
    "index()",
    "Returns the lowest match index or raises ValueError.",
    "text.index(sub)",
    "print('banana'.index('na'))",
    "2",
  ],
  [
    "count()",
    "Counts non-overlapping occurrences.",
    "text.count(sub)",
    "print('banana'.count('an'))",
    "2",
  ],
  [
    "startswith()",
    "Tests whether text starts with a prefix.",
    "text.startswith(prefix)",
    "print('python.py'.startswith('py'))",
    "True",
  ],
  [
    "endswith()",
    "Tests whether text ends with a suffix.",
    "text.endswith(suffix)",
    "print('python.py'.endswith('.py'))",
    "True",
  ],
  [
    "isalpha()",
    "Tests that all characters are alphabetic and text is non-empty.",
    "text.isalpha()",
    "print('Python'.isalpha())",
    "True",
  ],
  [
    "isdigit()",
    "Tests that all characters are digits and text is non-empty.",
    "text.isdigit()",
    "print('204'.isdigit())",
    "True",
  ],
  [
    "isalnum()",
    "Tests that all characters are alphanumeric and text is non-empty.",
    "text.isalnum()",
    "print('Room204'.isalnum())",
    "True",
  ],
].map(([name, purpose, syntax, example, result]) => ({
  name,
  purpose,
  syntax,
  example,
  result,
}));

const dictMethods: MethodReference[] = [
  [
    "get()",
    "Returns a key's value or a default without raising KeyError.",
    "dict.get(key, default=None)",
    "print({'a': 1}.get('b', 0))",
    "0",
  ],
  [
    "keys()",
    "Returns a dynamic view of keys.",
    "dict.keys()",
    "print(list({'a': 1}.keys()))",
    "['a']",
  ],
  [
    "values()",
    "Returns a dynamic view of values.",
    "dict.values()",
    "print(list({'a': 1}.values()))",
    "[1]",
  ],
  [
    "items()",
    "Returns a dynamic view of key-value pairs.",
    "dict.items()",
    "print(list({'a': 1}.items()))",
    "[('a', 1)]",
  ],
  [
    "update()",
    "Merges key-value pairs in place.",
    "dict.update(other)",
    "d={'a':1}; d.update({'b':2}); print(d)",
    "{'a': 1, 'b': 2}",
  ],
  [
    "setdefault()",
    "Returns a value, inserting a default when the key is absent.",
    "dict.setdefault(key, default=None)",
    "d={}; print(d.setdefault('items', []), d)",
    "[] {'items': []}",
  ],
  [
    "pop()",
    "Removes a key and returns its value.",
    "dict.pop(key[, default])",
    "d={'a':1}; print(d.pop('a'), d)",
    "1 {}",
  ],
  [
    "popitem()",
    "Removes and returns the newest key-value pair.",
    "dict.popitem()",
    "d={'a':1,'b':2}; print(d.popitem())",
    "('b', 2)",
  ],
  [
    "copy()",
    "Returns a shallow copy.",
    "dict.copy()",
    "d={'a':1}; print(d.copy())",
    "{'a': 1}",
  ],
  [
    "clear()",
    "Removes all items in place.",
    "dict.clear()",
    "d={'a':1}; d.clear(); print(d)",
    "{}",
  ],
].map(([name, purpose, syntax, example, result]) => ({
  name,
  purpose,
  syntax,
  example,
  result,
}));

const setMethods: MethodReference[] = [
  [
    "add()",
    "Adds one hashable element.",
    "set.add(element)",
    "s={1}; s.add(2); print(s == {1,2})",
    "True",
  ],
  [
    "update()",
    "Adds items from one or more iterables.",
    "set.update(*iterables)",
    "s={1}; s.update([2,3]); print(s == {1,2,3})",
    "True",
  ],
  [
    "remove()",
    "Removes an element or raises KeyError.",
    "set.remove(element)",
    "s={1,2}; s.remove(2); print(s)",
    "{1}",
  ],
  [
    "discard()",
    "Removes an element if present without raising for absence.",
    "set.discard(element)",
    "s={1}; s.discard(9); print(s)",
    "{1}",
  ],
  [
    "union()",
    "Returns elements present in either set.",
    "set.union(*others)",
    "print({1,2}.union({2,3}))",
    "{1, 2, 3}",
  ],
  [
    "intersection()",
    "Returns elements common to all sets.",
    "set.intersection(*others)",
    "print({1,2}.intersection({2,3}))",
    "{2}",
  ],
  [
    "difference()",
    "Returns elements absent from the other sets.",
    "set.difference(*others)",
    "print({1,2}.difference({2,3}))",
    "{1}",
  ],
  [
    "symmetric_difference()",
    "Returns elements present in exactly one set.",
    "set.symmetric_difference(other)",
    "print({1,2}.symmetric_difference({2,3}))",
    "{1, 3}",
  ],
].map(([name, purpose, syntax, example, result]) => ({
  name,
  purpose,
  syntax,
  example,
  result,
}));

const tupleMethods: MethodReference[] = [
  {
    name: "count()",
    purpose: "Counts values equal to the argument.",
    syntax: "tuple.count(value)",
    example: "print((1, 2, 1).count(1))",
    result: "2",
  },
  {
    name: "index()",
    purpose: "Returns the first index of a value.",
    syntax: "tuple.index(value, start=0, stop=len(tuple))",
    example: "print(('a', 'b').index('b'))",
    result: "1",
    note: "Raises ValueError when the value is absent.",
  },
];

export const dataStructureArticles = [
  topicArticle({
    title: "Strings",
    definition: "A string is an immutable sequence of Unicode characters.",
    category: "Data Structures",
    syntax: "text = 'Python'",
    example: "text = 'Python'\nprint(text[0], text[-1], text[1:4])",
    output: "P n yth",
    details: [
      "Indexes start at zero and negative indexes count from the end.",
      "Slicing returns a new string and excludes the stop index.",
      "String methods return new values because strings cannot be changed in place.",
    ],
    why: "Strings represent names, messages, file content, identifiers, and other text.",
    realWorld: "Normalize a username and validate a file suffix.",
    related: ["String Methods", "Regular Expressions", "Input and Output"],
    methods: stringMethods,
    mistakes: [
      "Calling index() when -1 from find() is the desired absence signal.",
      "Forgetting that join() is called on the separator.",
    ],
    keywords: [
      "str",
      "indexing",
      "slicing",
      "immutable",
      ...stringMethods.map((x) => x.name),
    ],
  }),
  topicArticle({
    title: "Lists",
    definition:
      "A list is a mutable, ordered sequence that can store references to values of any type.",
    category: "Data Structures",
    kind: "Data Structure",
    syntax: "items = [value1, value2]",
    example:
      "fruits = ['Apple', 'Mango', 'Banana']\nfruits.append('Orange')\nfruits[1] = 'Pear'\nprint(fruits[-2:])",
    output: "['Banana', 'Orange']",
    details: [
      "Create an empty list with [] or list(); lists allow duplicates and mixed types.",
      "Use zero-based and negative indexing, slicing, membership with in, len(), and for iteration.",
      "Assignment changes an element; append/extend/insert add items; remove/pop/del remove them.",
      "Nested lists hold other lists; a shallow copy duplicates only the outer container.",
      "Comprehensions build lists from transforms and filters; sort() mutates while sorted() returns a new list.",
    ],
    why: "Lists suit ordered collections that grow, shrink, or change while a program runs.",
    realWorld:
      "Maintain a to-do queue, shopping-cart lines, or collected sensor readings.",
    related: ["List Methods", "Tuples", "Comprehensions", "Shallow Copy"],
    methods: listMethods,
    mistakes: [
      "Accessing index len(list), which is one past the last element.",
      "Assigning result = values.sort(); sort() returns None.",
      "Using [[0] * 3] * 3 when independent nested rows are required.",
    ],
    practices: [
      "Use enumerate() instead of manually maintaining an index.",
      "Use a comprehension for a simple transformation, not complex side effects.",
      "Copy deliberately when independent mutation is required.",
    ],
    keywords: [
      "list",
      "index",
      "slice",
      "nested",
      "copy",
      "sort",
      ...listMethods.map((x) => x.name),
    ],
    interviewNote:
      "Lists are mutable dynamic arrays with average O(1) append and indexing; inserting near the front is O(n).",
  }),
  topicArticle({
    title: "Tuples",
    definition: "A tuple is an immutable, ordered sequence.",
    category: "Data Structures",
    kind: "Data Structure",
    syntax: "point = (3, 4)\nsingle = (3,)",
    example: "record = ('Asha', 91)\nname, score = record\nprint(name, score)",
    output: "Asha 91",
    details: [
      "A one-item tuple needs a trailing comma; parentheses alone do not create it.",
      "Tuples support indexing, slicing, unpacking, count(), and index().",
      "A tuple can be a dictionary key only when all of its elements are hashable.",
    ],
    why: "Tuples communicate fixed structure and can provide hashable compound keys.",
    realWorld:
      "Return a coordinate pair or use (country, code) as a mapping key.",
    related: ["Lists", "Dictionaries", "Mutable vs Immutable"],
    methods: tupleMethods,
    keywords: ["tuple", "unpacking", "immutable", "hashable"],
  }),
  topicArticle({
    title: "Sets",
    definition:
      "A set is a mutable, unordered collection of distinct hashable elements.",
    category: "Data Structures",
    kind: "Data Structure",
    syntax: "values = {1, 2}\nempty = set()",
    example:
      "required = {'read', 'write'}\ngranted = {'read', 'admin'}\nprint(required & granted)",
    output: "{'read'}",
    details: [
      "Sets remove duplicates and optimize membership tests.",
      "They support union, intersection, difference, and symmetric difference.",
      "Sets are not indexed; iteration order must not be used as a stable position.",
    ],
    why: "Sets express membership and mathematical collection relationships directly.",
    realWorld: "Find shared permissions or unique visitor identifiers.",
    related: ["Frozenset", "Lists", "Dictionaries"],
    methods: setMethods,
    mistakes: [
      "Using {} for an empty set; it creates a dictionary.",
      "Trying to add a mutable list as an element.",
    ],
    keywords: [
      "set",
      "unique",
      "hashable",
      "union",
      "intersection",
      ...setMethods.map((x) => x.name),
    ],
  }),
  topicArticle({
    title: "Dictionaries",
    definition:
      "A dictionary is a mutable mapping from unique hashable keys to values.",
    category: "Data Structures",
    kind: "Data Structure",
    syntax: "mapping = {key: value}",
    example:
      "student = {'name': 'Asha', 'score': 91}\nstudent['passed'] = student['score'] >= 40\nprint(student['passed'])",
    output: "True",
    details: [
      "Dictionary lookup is by key, not numeric position; insertion order is preserved.",
      "Assigning an existing key replaces its value; assigning a new key adds a pair.",
      "keys(), values(), and items() return dynamic view objects.",
    ],
    why: "Mappings model records, indexes, configuration, and fast key-based lookup.",
    realWorld: "Represent a student record or index products by SKU.",
    related: ["Dictionary Methods", "Sets", "JSON"],
    methods: dictMethods,
    mistakes: [
      "Indexing a missing key instead of using get() when absence is normal.",
      "Using a mutable list as a key.",
    ],
    keywords: [
      "dict",
      "mapping",
      "key",
      "value",
      "insertion order",
      ...dictMethods.map((x) => x.name),
    ],
  }),
];

const typeRows: ReferenceRow[] = [
  [
    "int",
    "Immutable integer; ordered numerically; arithmetic and bitwise operations.",
    "42",
    "Counts and exact whole numbers",
  ],
  [
    "float",
    "Immutable binary floating-point number.",
    "3.14",
    "Measurements and approximate numeric work",
  ],
  [
    "complex",
    "Immutable number with real and imaginary parts.",
    "2 + 3j",
    "Scientific calculations",
  ],
  [
    "str",
    "Immutable ordered Unicode sequence; duplicates and indexing allowed.",
    "'Python'",
    "Human-readable text",
  ],
  [
    "bool",
    "Immutable truth value; bool is a subclass of int.",
    "True",
    "Conditions and flags",
  ],
  [
    "list",
    "Mutable ordered sequence; duplicates and indexing allowed.",
    "[1, 2]",
    "Changing ordered collections",
  ],
  [
    "tuple",
    "Immutable ordered sequence; duplicates and indexing allowed.",
    "(1, 2)",
    "Fixed records and hashable composites",
  ],
  [
    "set",
    "Mutable collection of unique hashable elements; no indexing.",
    "{1, 2}",
    "Membership and set algebra",
  ],
  [
    "frozenset",
    "Immutable set of unique hashable elements.",
    "frozenset({1, 2})",
    "Hashable set values",
  ],
  [
    "dict",
    "Mutable insertion-ordered key-value mapping; keys are unique.",
    "{'a': 1}",
    "Records and lookup tables",
  ],
  [
    "range",
    "Immutable arithmetic progression with lazy storage.",
    "range(5)",
    "Integer iteration",
  ],
  [
    "bytes",
    "Immutable sequence of integers from 0 to 255.",
    "b'ABC'",
    "Encoded or binary data",
  ],
  [
    "bytearray",
    "Mutable sequence of bytes.",
    "bytearray(b'ABC')",
    "Editable binary buffers",
  ],
  [
    "NoneType",
    "The type of the singleton None, representing no value.",
    "None",
    "Missing or absent result",
  ],
].map(([label, meaning, example, result]) => ({
  label,
  meaning,
  example,
  result,
}));

export const dataTypeReference = topicArticle({
  title: "Built-in Data Type Reference",
  definition:
    "Python's built-in types cover numbers, text, binary data, sequences, sets, mappings, ranges, and absence.",
  category: "Data Structures",
  kind: "Syntax",
  syntax: "type(value)\nisinstance(value, expected_type)",
  example: "value = frozenset({1, 2})\nprint(type(value).__name__, len(value))",
  output: "frozenset 2",
  details: [
    "Mutability describes whether an object's state can change after creation.",
    "Order and indexing are separate: dictionaries preserve insertion order but are accessed by key.",
    "Hashable values may be dictionary keys or set elements; most mutable containers are not hashable.",
  ],
  why: "A deliberate type choice makes valid operations, performance, and intent easier to reason about.",
  realWorld:
    "Choose a list for editable order, a set for membership, and a dictionary for keyed records.",
  related: ["Data Types", "Mutable vs Immutable", "Lists", "Dictionaries"],
  operations: typeRows,
  keywords: typeRows.flatMap((x) => [x.label, x.meaning]),
});

export const methodOverviewArticles = [
  [
    "List Methods",
    "Methods that inspect or mutate Python lists.",
    listMethods,
    "Lists",
  ],
  [
    "String Methods",
    "Methods that return inspected or transformed string values.",
    stringMethods,
    "Strings",
  ],
  [
    "Dictionary Methods",
    "Methods for mapping lookup, views, updates, and removal.",
    dictMethods,
    "Dictionaries",
  ],
  [
    "Set Methods",
    "Methods for membership updates and mathematical set operations.",
    setMethods,
    "Sets",
  ],
  [
    "Tuple Methods",
    "The small method set available on immutable tuples.",
    tupleMethods,
    "Tuples",
  ],
].map(([title, definition, methods, owner]) =>
  topicArticle({
    title: title as string,
    definition: definition as string,
    category: "Python Methods",
    kind: "Method",
    syntax: (methods as MethodReference[]).map((x) => x.syntax).join("\n"),
    example: (methods as MethodReference[])[0].example,
    output: (methods as MethodReference[])[0].result,
    details: [
      `Open any method card for parameters, return behavior, an example, and common mistakes.`,
      `Methods may mutate their receiver or return a new value; the distinction is documented per method.`,
      `Edge cases such as missing values and empty collections can raise specific exceptions.`,
    ],
    why: `A method-focused index makes the ${String(owner).toLowerCase()} API quick to scan.`,
    realWorld: `Choose a direct ${String(owner).toLowerCase()} operation while transforming application data.`,
    related: [owner as string],
    methods: methods as MethodReference[],
    keywords: (methods as MethodReference[]).flatMap((x) => [
      x.name,
      x.purpose,
    ]),
  }),
);

export const methodArticles = [
  ...listMethods.map((x) => methodArticle("List", x, ["Lists"])),
  ...stringMethods.map((x) => methodArticle("String", x, ["Strings"])),
  ...dictMethods.map((x) => methodArticle("Dictionary", x, ["Dictionaries"])),
  ...setMethods.map((x) => methodArticle("Set", x, ["Sets"])),
  ...tupleMethods.map((x) => methodArticle("Tuple", x, ["Tuples"])),
];
